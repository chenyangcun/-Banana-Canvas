import type { Adjustments } from './types.ts';

export const AUTOSAVE_KEY = 'ai-image-editor-autosave-metadata';

export const DEFAULT_ADJUSTMENTS: Adjustments = { brightness: 100, contrast: 100, saturation: 100 };